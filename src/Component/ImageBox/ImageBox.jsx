import styles from "./ImageBox.module.scss";

// eslint-disable-next-line react/prop-types
const ImageBox = ({ children }) => {
  return (
    
      <div className={styles.chi}> {children}</div>

  );
};

export default ImageBox;
